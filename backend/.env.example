PORT=5000
JWT_SECRET=your_super_secret_jwt_key_change_this
CLIENT_URL=http://localhost:5173

# Option 1: JSON file path (local dev) — put firebase-service-account.json in backend root
# Option 2: Paste full JSON as string (production/hosting)
# FIREBASE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"..."}
